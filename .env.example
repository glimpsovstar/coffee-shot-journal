# Copy to .env.local and restart the dev server. Never commit .env.local.
#
# Demo-only: label scan calls OpenAI from the browser. For production, use a
# backend proxy (see GitHub issue: secure label scanning).

# VITE_OPENAI_API_KEY=sk-your-key-here
